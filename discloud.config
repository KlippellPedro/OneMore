NAME=OneMore
TYPE=site
ID=onemore
MAIN=servidor.js
# sem BUILD: a Discloud ja roda `npm install` e `npm run build` sozinha no deploy,
# e e o `build` do package.json que gera o dist/ que o servidor.js serve.
RAM=512
VERSION=latest
AUTORESTART=true
START=node servidor.js
